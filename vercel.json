{
  "installCommand": "npm install --force",
  "buildCommand": "rm -rf node_modules/.vite dist && npm run build",
  "rewrites": [
    {
      "source": "/api/piped",
      "destination": "/api/piped"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
