{
  "installCommand": "npm install --force",
  "buildCommand": "rm -rf node_modules/.vite node_modules/.cache dist .vite && npm run build",
  "rewrites": [
    {
      "source": "/api/piped",
      "destination": "/api/piped"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
